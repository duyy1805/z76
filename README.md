# Sổ séc điện tử Z76

Frontend React + Vite cho quy trình lập, trình duyệt và quản lý phiếu séc.

## Chạy dự án

```bash
npm install
npm run dev
```

## Kiểm tra production build

```bash
npm run build
```
